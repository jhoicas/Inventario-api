# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  .env.example — Variables de entorno de producción                          ║
# ║                                                                              ║
# ║  1. Copia este archivo:  cp .env.example .env.prod                          ║
# ║  2. Rellena TODOS los valores (no dejes ninguno con el placeholder).         ║
# ║  3. NUNCA hagas commit de .env.prod  (ya está en .gitignore).               ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# ── Aplicación ────────────────────────────────────────────────────────────────
APP_ENV=production
APP_NAME=invorya-erp
APP_VERSION=1.0.0

# ── Base de datos (Supabase / PostgreSQL) ─────────────────────────────────────
# Usa la connection string con el password URL-encoded
# Caracteres especiales: # → %23  * → %2A  & → %26  % → %25  @ → %40
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require

# ── HTTP ───────────────────────────────────────────────────────────────────────
HTTP_HOST=0.0.0.0
HTTP_PORT=8080

# ── JWT ────────────────────────────────────────────────────────────────────────
# Generar con: openssl rand -base64 64
JWT_SECRET=CAMBIAR_POR_UN_SECRET_SEGURO_DE_64_BYTES
JWT_EXPIRATION_MINUTES=60
JWT_ISSUER=invorya-erp

# ── DIAN ───────────────────────────────────────────────────────────────────────
DIAN_TECHNICAL_KEY=CLAVE_TECNICA_DE_LA_RESOLUCION_DIAN
# 1 = Producción | 2 = Pruebas/Habilitación
DIAN_ENVIRONMENT=2
# dev | test | prod
DIAN_ENV=dev

# Certificado digital (.p12/.pfx) para firma XAdES-EPES
# Ruta dentro del contenedor (montar el archivo con un volume secreto)
DIAN_CERT_PATH=/run/secrets/dian_cert.p12
DIAN_CERT_PASSWORD=CONTRASEÑA_DEL_CERTIFICADO

# ── Anthropic AI ──────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-TU_CLAVE_AQUI
ANTHROPIC_MODEL=claude-3-5-haiku-20241022
